#include "ProjectSharedConfig.h"
#include "Log.h"

namespace AM
{

SharedConfig ProjectSharedConfig::getEngineSettings() 
{
    SharedConfig config{};

    // Note: Currently just using the defaults for everything.
    return config;
}

} // End namespace AM
